{
  "name": "ec2-info",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -r node_modules && npm i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mark Greenwood",
  "license": "UNLICENSED",
  "dependencies": {
    "aws-sdk": "^2.339.0",
    "ramda": "^0.25.0"
  }
}
